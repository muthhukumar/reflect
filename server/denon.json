{
  "$schema": "https://deno.land/x/denon/schema.json",
  "scripts": {
    "start": {
      "cmd": "deno run --allow-net --allow-env --allow-read --allow-write --allow-plugin --unstable mod.ts",
      "desc": "run my app.ts file"
    }
  }
}
